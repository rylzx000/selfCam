# selfCam 版本信息

## 当前版本

**版本号**: v1.3.4
**发布日期**: 2026-04-29
**状态**: 已封板（本地）

---

## 版本概述

`v1.3.4` 是基于 `v1.3.3` 的权限与相册保存瘦身版本。重点收敛首页开始采集权限申请、相册保存失败轻提示、确认照片保存不阻断主流程，并删除 backgroundFetch 误判处理和保存成功提示绕过逻辑。

### 本版本重点

- 首页开始采集保留相机必需权限与相册可选权限申请。
- 相机权限拒绝时不进入拍摄页；相册权限拒绝时继续原拍摄流程。
- 确认使用后尝试保存当前照片到手机相册，保存成功不调用业务成功 toast。
- 保存失败不阻断确认、缓存、跳转和预览流程，仅提示“照片未保存到相册，不影响拍摄”。
- 重拍照片仍不保存。
- 首页开始采集保留防重复点击，避免连续点击导致重复跳转。
- 确认项目没有主动调用 backgroundFetch 相关 API，不新增相关处理。

## v1.3.4 变更摘要

### 权限与相册保存

- 简化 `utils/permission.js`，返回 `{ cameraGranted, albumGranted }`，删除权限状态记录、相册权限弹窗和冗余分支。
- 简化 `utils/album.js`，仅负责路径选择、调用 `wx.saveImageToPhotosAlbum`、失败轻提示和权限拒绝静默。
- `pages/camera/camera.js` 在确认使用时 fire-and-forget 保存相册，失败不阻断照片确认和后续流程。
- `pages/index/index.js` 保留开始采集防重复点击和异常轻提示。

### 测试与文档同步

- `package.json` 与 `package-lock.json` 版本号同步至 `1.3.4`。
- 补充权限、相册保存、首页防重复点击和重拍不保存相关测试。
- 本轮验证通过 `npm test -- --runInBand`。

---

## 历史版本

| 版本 | 发布日期 | 状态 | 说明 |
| --- | --- | --- | --- |
| v1.3.4 | 2026-04-29 | 已封板（本地） | 权限申请与相册保存瘦身，保留失败轻提示和开始采集防重复点击 |
| v1.3.3 | 2026-04-28 | 已封板（本地） | 补齐微信开发者工具 miniprogram-automator + Jest 自动化测试入口，覆盖首批回归与破坏性测试场景 |
| v1.3.2 | 2026-04-28 | 已封板（本地） | 修复预览页车损补拍回流和车损 AI 首次启动时机，同步 VIN 引导与确认态文案 |
| v1.3.1 | 2026-04-28 | 已封板（本地） | 首页横屏单屏品牌入口，补齐 logo 资源与版本/PRDS/测试文档同步 |
| v1.2.6 | 2026-04-28 | 已封板（本地） | 完成页轻量收口，统计卡片改为车辆数、车损照片和单证照片 |
| v1.2.5 | 2026-04-27 | 已封板（本地） | 环境配置与调试开关收口，补齐 env-config 测试与审查工作流规则 |
| v1.2.4 | 2026-04-26 | 已封板（本地） | 端上轻质检拍后分析模块落地，补齐测试、设计文档与 disabled 语义修正 |
| v1.2.3 | 2026-04-24 | 已封板（本地） | 端上轻质检三层配置体系落地，补齐环境策略、缓存降级、测试与设计文档 |
| v1.2.2 | 2026-04-24 | 已封板（本地） | workflow-state 收敛、本地缓存治理、异常链路测试与测试文档落地 |
| v1.2.1 | 2026-04-24 | 已封板 | 前端状态机骨架接入、恢复收紧与单证流程修正 |
| v1.2.0 | 2026-04-23 | 已封板 | 车损距离引导与车牌真机流畅度优化 |
| v1.1.0 | 2026-04-22 | 已归档 | AI 自动拍照增强版，完成本地封板 |
| v1.0.0 | 2026-04-01 | 已归档 | 首个正式版本 |

---

## 回滚方式

当前推荐优先回滚到已经存在的 Git 标签，例如：

```powershell
git fetch --tags
git checkout v1.3.3
```

如果后续需要把 `v1.3.4` 作为正式标签发布，建议在本地提交后再创建对应 tag。

---

*最后更新：2026-04-29*
