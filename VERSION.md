# selfCam 版本信息

## 当前版本

**版本号**: v1.3.1  
**发布日期**: 2026-04-28  
**状态**: 已封板（本地）

---

## 版本概述

`v1.3.1` 是基于 `v1.2.6` 的首页业务调整版本，也是 `v1.3.x` 系列的起点。核心目标是把首页按横屏样式稿收口到更接近交付视觉的单屏品牌入口，同时保持现有启动逻辑、横屏配置和拍照主流程不变。

### 本版本重点

- 首页改为横屏单屏布局：左上角固定品牌 logo，中部展示标题、副标题和拍摄须知，底部保留开始入口与权限提示。
- 开始按钮文案统一为“开始采集”，继续复用原首页 `onStart` 事件与启动逻辑，不改业务流程。
- 新增 `assets/logo.png` 作为首页品牌资源，并同步更新 `PRDS`、测试文档和版本记录。

---

## v1.3.1 变更摘要

### 首页横屏改版

- `pages/index/index.wxml` 按横屏样式稿重建首页结构，包含左上品牌 logo、中部标题/副标题、拍摄须知卡片、`开始采集` 按钮和底部权限提示。
- `pages/index/index.wxss` 重写首页横屏单屏样式，容器固定 `100vw / 100vh`，禁用纵向滚动，保证首页元素在横屏下一屏展示。
- `assets/logo.png` 作为首页品牌资源纳入版本交付。

### 版本与文档同步

- `package.json` 版本号提升至 `1.3.1`，明确后续按 `1.3.x` 承接业务需求调整。
- 更新 `PRDS/PRD.md`、`PRDS/UI.md`、`PRDS/tech.md` 与 `PRDS/auto-capture-ai.md` 的代码基线，并同步首页最新描述。
- 更新 `docs/test-cases.md` 与 `docs/ai-auto-capture-test-cases.md` 的代码基线，其中首页测试用例同步改为校验“开始采集”入口和新版首页元素。

### 保持不变

- 不修改 `app.json`、`app.wxss`、其他页面和首页启动逻辑。
- 继续使用原有 `onStart` 点击事件进入拍照主流程。

---

## 历史版本

| 版本 | 发布日期 | 状态 | 说明 |
| --- | --- | --- | --- |
| v1.3.1 | 2026-04-28 | 已封板（本地） | 首页按横屏样式稿重构为单屏品牌入口，补齐 logo 资源与版本/PRDS/测试文档同步 |
| v1.2.6 | 2026-04-28 | 已封板（本地） | 完成页轻量收口，统计卡片改为车辆数/车损照片/单证照片，并同步摘要字段、测试与 PRDS 文档 |
| v1.2.5 | 2026-04-27 | 已封板（本地） | 环境配置与调试开关收口，补齐 env-config 测试与审查工作流规则 |
| v1.2.4 | 2026-04-26 | 已封板（本地） | 端上轻质检拍后分析模块落地，补齐测试、设计文档与 disabled 语义修正 |
| v1.2.3 | 2026-04-24 | 已封板（本地） | 端上轻质检三层配置体系落地，补齐环境策略、缓存降级、测试与设计文档 |
| v1.2.2 | 2026-04-24 | 已封板（本地） | workflow-state 收敛、本地缓存治理三步补齐、异常链路测试与测试文档落地 |
| v1.2.1 | 2026-04-24 | 已封板 | 前端状态机骨架接入、恢复收紧与单证流程修正 |
| v1.2.0 | 2026-04-23 | 已封板 | 车损距离引导与车牌真机流畅度优化 |
| v1.1.0 | 2026-04-22 | 已归档 | AI 自动拍照增强版，完成本地封板 |
| v1.0.0 | 2026-04-01 | 已归档 | 首个正式版本 |

---

## 回滚方式

当前推荐优先回滚到已经存在的 Git 标签，例如：

```powershell
git fetch --tags
git checkout v1.2.6
```

如果后续需要把 `v1.3.1` 作为正式标签发布，建议在本地提交后再创建对应 tag。

---

## 下一步版本建议

建议后续继续按 `v1.3.x` 业务需求版本递增，优先考虑：

- 继续验证横屏首页在不同机型下的 logo、标题、须知卡片和按钮是否都能稳定一屏展示。
- 若后续首页继续承接业务需求入口，优先保持品牌露出、信息层级和主流程入口的简洁性。
- 在不改主流程前提下，补齐真机灰度验证与生产配置回归检查。

---

*最后更新：2026-04-28*
