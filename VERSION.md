# selfCam 版本信息

## 当前版本

**版本号**: v1.3.5
**发布日期**: 2026-04-30
**状态**: 已封板（本地）

---

## 版本概述

`v1.3.5` 是基于 `v1.3.4` 的预览页行驶证资料上传版本。重点实现每辆车独立上传行驶证资料、实体/电子行驶证模式切换、缓存兼容、删除/替换/预览、完成前风险提示，并统一初始化页、拍照页和预览页的浅灰绿色背景。

### 本版本重点

- 每辆车增加通用 `documents[]` 与 `documentSelections` 单证结构，行驶证不再写死为单独字段。
- 预览页每辆车照片列表顺序调整为 `车牌 -> VIN -> 车损 -> 行驶证资料`。
- 支持实体行驶证正页/副页、电子行驶证单张上传，来源支持拍照和相册。
- 行驶证图片支持真实缩略图展示、全屏查看、删除和重新上传替换。
- 完成采集时先确认是否还需添加三者车，再对未完成行驶证车辆弹一次风险提示。
- 初始化页提示文案更新为提前准备所有事故车辆行驶证，支持 12123 电子版。
- 拍照页背景统一为初始化页和预览页同款浅灰绿色底色与右上淡绿色装饰。
- 通用确认弹窗的遮罩点击只关闭弹窗，不再触发取消按钮业务动作。

## v1.3.5 变更摘要

### 行驶证资料上传

- 新增 `utils/documents.js`，统一管理车辆级单证默认值、旧缓存兼容、行驶证完成态、上传槽位和预览数据。
- `utils/storage-schema.js` 升级到 schema v2，旧车辆缺少 `documents` 或 `documentSelections` 时自动补默认值。
- `utils/storage.js` 新增车辆级单证模式保存、图片写入替换和删除能力。
- `utils/cache-selectors.js` 将车辆级行驶证图片纳入预览页照片列表、完成页单证统计和质量汇总。
- `utils/compress.js` 支持按调用方传入 `maxFileSize`，行驶证上传目标约 400KB，原车辆照片默认策略不变。

### 预览页交互

- 预览页删除独立单证模块展示，将行驶证资料放在每辆车照片列表末尾。
- 行驶证未完成时显示 `上传行驶证` 入口；完成后显示真实原图缩略图。
- 行驶证上传面板支持实体/电子切换，切换不删除已上传图片。
- 上传位已上传后可查看、重新上传或删除。
- 完成采集弹窗顺序调整为先确认是否添加三者车，再判断行驶证是否齐全。
- 行驶证风险提示文案调整为定损金额准确性风险，确认按钮为 `确认提交`。

### 测试与文档同步

- `package.json` 与 `package-lock.json` 版本号同步至 `1.3.5`。
- 新增 `vehicle-documents` 与 `preview-driving-license` 测试，覆盖旧缓存兼容、实体/电子完成态、上传替换删除、相册保存、风险提示和串车隔离。
- 本轮验证通过 `npm test -- --runInBand`。

---

## 历史版本

| 版本 | 发布日期 | 状态 | 说明 |
| --- | --- | --- | --- |
| v1.3.5 | 2026-04-30 | 已封板（本地） | 每辆车行驶证资料上传、缓存兼容、提交风险提示与三页背景统一 |
| v1.3.4 | 2026-04-29 | 已封板（本地） | 权限申请与相册保存瘦身，保留失败轻提示和开始采集防重复点击 |
| v1.3.3 | 2026-04-28 | 已封板（本地） | 补齐微信开发者工具 miniprogram-automator + Jest 自动化测试入口，覆盖首批回归与破坏性测试场景 |
| v1.3.2 | 2026-04-28 | 已封板（本地） | 修复预览页车损补拍回流和车损 AI 首次启动时机，同步 VIN 引导与确认态文案 |
| v1.3.1 | 2026-04-28 | 已封板（本地） | 首页横屏单屏品牌入口，补齐 logo 资源与版本/PRDS/测试文档同步 |
| v1.2.6 | 2026-04-28 | 已封板（本地） | 完成页轻量收口，统计卡片改为车辆数、车损照片和单证照片 |
| v1.2.5 | 2026-04-27 | 已封板（本地） | 环境配置与调试开关收口，补齐 env-config 测试与审查工作流规则 |
| v1.2.4 | 2026-04-26 | 已封板（本地） | 端上轻质检拍后分析模块落地，补齐测试、设计文档与 disabled 语义修正 |
| v1.2.3 | 2026-04-24 | 已封板（本地） | 端上轻质检三层配置体系落地，补齐环境策略、缓存降级、测试与设计文档 |
| v1.2.2 | 2026-04-24 | 已封板（本地） | workflow-state 收敛、本地缓存治理、异常链路测试与测试文档落地 |
| v1.2.1 | 2026-04-24 | 已封板 | 前端状态机骨架接入、恢复收紧与单证流程修正 |
| v1.2.0 | 2026-04-23 | 已封板 | 车损距离引导与车牌真机流畅度优化 |
| v1.1.0 | 2026-04-22 | 已归档 | AI 自动拍照增强版，完成本地封板 |
| v1.0.0 | 2026-04-01 | 已归档 | 首个正式版本 |

---

## 回滚方式

当前推荐优先回滚到已经存在的 Git 标签，例如：

```powershell
git fetch --tags
git checkout v1.3.4
```

如果后续需要把 `v1.3.5` 作为正式标签发布，建议在本地提交后再创建对应 tag。

---

*最后更新：2026-04-30*
