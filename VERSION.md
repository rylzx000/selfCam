# selfCam 版本信息

## 当前版本

**版本号**: v1.2.6  
**发布日期**: 2026-04-28  
**状态**: 已封板（本地）

---

## 版本概述

`v1.2.6` 是基于 `v1.2.5` 的一次轻量收口补丁，核心目标是把完成页恢复到更接近原始交付形态的简洁成功页，同时补齐与当前实现一致的缓存摘要字段和文档描述。此次版本不接后端接口、不改拍摄主流程，只优化完成页展示信息和文档版本同步。

### 本版本重点

- 完成页保留原始横屏居中成功页结构，只将白色统计框收口为“车辆数 / 车损照片 / 单证照片”三项。
- `cache-selectors` 新增 `damagePhotoCount` 与 `documentPhotoCount` 两个摘要字段，页面层可以直接读取，不再自己拆照片分类统计。
- 移除完成页当前实现中未使用的质量提示卡片依赖，保持“拍摄完成”页简洁稳定。
- 补齐完成页轻量展示和返回/退出行为测试，并同步更新 `PRDS/PRD.md`、`PRDS/UI.md`、`PRDS/tech.md`。

---

## v1.2.6 变更摘要

### 完成页轻量优化

- `pages/complete/complete.wxml` 保持原始完成页结构，只把统计卡片从“车辆数 / 照片数”调整为“车辆数 / 车损照片 / 单证照片”。
- `pages/complete/complete.wxss` 仅调整统计卡片内边距和最小列宽，避免三项统计挤压，同时保留原始按钮和中心布局。
- `pages/complete/complete.js` 读取 `vehicleCount`、`damagePhotoCount`、`documentPhotoCount` 并保留旧 `photoCounts` 的兼容回退。

### 摘要字段与测试

- `utils/cache-selectors.js` 在 `getCacheSummary(cache)` 中补充 `damagePhotoCount` 和 `documentPhotoCount`。
- 新增 `__tests__/complete-page.test.js`，覆盖完成页展示映射、返回修改不清缓存和完成退出直接清缓存。
- 更新 `__tests__/cache-selectors.test.js` 与 `__tests__/cache-selectors.edge.test.js`，校验新增摘要字段在正常和边界场景下都稳定返回。

### 文档同步

- 更新 `PRDS/PRD.md`、`PRDS/UI.md`、`PRDS/tech.md` 中关于完成页统计项和轻提示的描述，使其与当前实现一致。

---

## 历史版本

| 版本 | 发布日期 | 状态 | 说明 |
| --- | --- | --- | --- |
| v1.2.6 | 2026-04-28 | 已封板（本地） | 完成页轻量收口，统计卡片改为车辆数/车损照片/单证照片，并同步摘要字段、测试与 PRDS 文档 |
| v1.2.5 | 2026-04-27 | 已封板（本地） | 环境配置与调试开关收口，补齐 env-config 测试与审查工作流规则 |
| v1.2.4 | 2026-04-26 | 已封板（本地） | 端上轻质检拍后分析模块落地，补齐测试、设计文档与 disabled 语义修正 |
| v1.2.3 | 2026-04-24 | 已封板（本地） | 端上轻质检三层配置体系落地，补齐环境策略、缓存降级、测试与设计文档 |
| v1.2.2 | 2026-04-24 | 已封板（本地） | workflow-state 收敛、本地缓存治理三步补齐、异常链路测试与测试文档落地 |
| v1.2.1 | 2026-04-24 | 已封板 | 前端状态机骨架接入、恢复收紧与单证流程修正 |
| v1.2.0 | 2026-04-23 | 已封板 | 车损距离引导与车牌真机流畅度优化 |
| v1.1.0 | 2026-04-22 | 已归档 | AI 自动拍照增强版，完成本地封板 |
| v1.0.0 | 2026-04-01 | 已归档 | 首个正式版本 |

---

## 回滚方式

当前推荐优先回滚到已经存在的 Git 标签，例如：

```powershell
git fetch --tags
git checkout v1.2.5
```

如果后续需要把 `v1.2.6` 作为正式标签发布，建议在本地提交后再创建对应 tag。

---

## 下一步版本建议

建议后续继续按 `v1.2.x` 补丁版本递增，优先考虑：

- 在不重做完成页的前提下，继续验证横屏真机下三指标卡片的可读性与空间利用率
- 继续收口剩余零散 `console` / 调试开关，避免模块自行判断环境
- 在不改主流程前提下，补齐真机灰度验证与生产配置回归检查

---

*最后更新：2026-04-28*
