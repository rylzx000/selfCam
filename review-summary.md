# 审查摘要

## 本次改动

- 在 `AGENTS.md` 增加“测试与验证分级”，约束默认只跑与改动直接相关的最小测试集。
- 明确小 UI、小交互、缓存/上传/跨页面、相机/特殊分辨率等场景分别对应的验证范围。
- 明确用户要求“不跑测试”时不运行测试，并在最终回复说明未运行。
- 增加常用验证命令选择，便于后续按风险快速选命令。

## 审查重点

- `review.diff` 只包含 `AGENTS.md` 的指令层变更。
- 本次不涉及业务代码、测试逻辑、接口文档或版本号。
- 本次未运行测试，因为只修改协作指令文档。
