## 1. 产品规则矩阵

- [x] 1.1 新增 `docs/product-rule-matrix.md`，覆盖总原则、四段流程、断点续传、异常场景、规则漂移点和下一轮测试修正建议
- [x] 1.2 在证件信息章节完整列出 `E/F/B` 八状态矩阵和测试层级建议
- [x] 1.3 在模块二章节明确每车最多 10 张，并标记 5 张满图或第 6 张拒绝为旧规则

## 2. 测试说明同步

- [x] 2.1 更新 `docs/test-flow-route-matrix.md`，声明流程路由矩阵以产品规则矩阵为规则事实源
- [x] 2.2 更新 `docs/test-run-guide.md`，标记当前已知规则漂移点和下一轮测试修正顺序

## 3. 验证

- [x] 3.1 运行 OpenSpec strict validation 验证 change 产物
- [x] 3.2 运行轻量文档检查和 `git diff --stat`，确认本轮未改业务代码和测试代码

## 4. 测试规则漂移修正

- [x] 4.1 修正车损 e2e 旧上限：以 10 张为满图并验证第 11 张拒绝或不新增
- [x] 4.2 修正证件入口 e2e 旧假设：入口先打开自定义面板，槽位内未上传项才弹原生 `actionSheet`
- [x] 4.3 修正模块三缺证件 e2e 旧 toast：断言交接弹层“待补充：证件信息”，并保留最终提交前证件风险确认
- [x] 4.4 扩展 `quality-guards`，扫描 e2e 和 Jest 测试实现中的明确旧规则短语
- [x] 4.5 同步测试说明文档中的漂移点状态，保留真实点击 smoke 为下一轮
- [x] 4.6 运行本轮轻中量验证，不运行重型 e2e

## 5. 真实点击 smoke 补强

- [x] 5.1 新增 `e2e/specs/real-click-smoke.spec.js`，覆盖模块三证件入口真实点击到自定义证件面板，再到槽位内拍照/相册 `actionSheet`
- [x] 5.2 覆盖模块二车损 `+` 真实点击进入车损拍摄，确认照片后回到 `mode=moduleTwo`
- [x] 5.3 覆盖最终预览车损删空后 `+` 真实点击补拍，确认照片后回到 `mode=final`
- [x] 5.4 覆盖 10 张车损上限真实渲染不再出现新增入口，以及模块三 `E+F` 状态真实点击副页 `+` 后可补齐
- [x] 5.5 同步测试说明文档和产品规则矩阵中的真实点击 smoke 覆盖状态
- [x] 5.6 运行本轮轻量验证；如本机 e2e 环境不可稳定运行，记录建议手动命令
- [x] 5.7 验证 `real-click-smoke.spec.js` 单文件可串行执行，并收敛 automator 启动、拍摄指引和自定义组件点击的测试稳定性问题
