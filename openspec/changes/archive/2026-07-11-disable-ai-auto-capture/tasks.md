## 1. 集中配置与运行短路

- [x] 1.1 在 `packageD/utils/ai-config.js` 增加默认关闭的 AI 业务总开关，并预留车牌与车损子开关。
- [x] 1.2 在拍照页分离 `aiFeatureEnabled`、`aiAvailable`、`aiEnabled`，统一判断当前步骤是否允许 AI。
- [x] 1.3 在 AI 初始化、检测器创建、检测恢复、实时帧监听和检测循环入口增加关闭状态短路，确保不创建模型、推理会话、自动捕获引擎或定时器。

## 2. 手动拍照界面与流程

- [x] 2.1 调整拍照页 AI 状态和车损提示，使关闭状态不展示 AI 文案且拍照按钮显示“点击拍照”。
- [x] 2.2 验证车牌、车损、模块一进入模块二、多车辆、补拍、确认、重拍、最多 10 张和完成弹层继续使用现有手动流程。

## 3. 自动化测试

- [x] 3.1 完善 `__tests__/camera-ai-start.test.js`，覆盖关闭时不创建或加载车牌、车损检测器，不下载模型、不创建推理会话，不启动帧监听、检测循环和自动拍照定时器。
- [x] 3.2 覆盖关闭时不展示 AI 状态、按钮文案正确，以及车牌、车损、模块切换和多车辆手动拍照流程。
- [x] 3.3 覆盖将总开关恢复为 `true` 后，现有 AI 初始化、设备能力判断和检测器加载逻辑仍可用。

## 4. 验证

- [x] 4.1 运行相关 Jest 测试和本次修改 JavaScript 文件的语法检查。
- [x] 4.2 运行 OpenSpec strict validation，并确认实现与 delta spec 一致。
